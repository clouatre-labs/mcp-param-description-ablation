[project]
name = "mcp-param-description-ablation"
version = "0.1.0"
description = "Harness for the MCP parameter-description ablation experiment (clouatre-labs/clouatre.ca#705)"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.69.0",
]

[dependency-groups]
dev = [
    "ruff>=0.9.0",
    "pyright>=1.1.0",
]
